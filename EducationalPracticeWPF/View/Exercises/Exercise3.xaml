<Page x:Class="EducationalPracticeWPF.View.Exercises.Exercise3"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Exercise3">
    <Grid>
        <DataGrid ItemsSource="{Binding Path=.}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Code" Binding="{Binding Path = Country.Code}"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Path = Country.Name}"/>
                <DataGridTextColumn Header="Year" Binding="{Binding Path = Year}"/>
                <DataGridTextColumn Header="Value" Binding="{Binding Path = Value}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
